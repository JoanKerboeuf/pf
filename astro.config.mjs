// astro.config.mjs

import { defineConfig } from 'astro/config';

export default defineConfig({
  // ... autres configurations ...

  devOptions: {
    port: 4321, // Change this to another available port if needed
  },
});
